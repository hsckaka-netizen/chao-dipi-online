{
  "name": "chao-dipi-online",
  "version": "0.1.0",
  "private": true,
  "description": "Online multiplayer Chao Dipi room prototype.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check account-auth.js && node --check public/app.js && node --check public/state-patch.js && node --check public/gameplay-effects.js && node --check game-evaluations.js && node --check game-history.js",
    "test": "node --test"
  },
  "dependencies": {
    "pg": "^8.16.3"
  },
  "engines": {
    "node": ">=20"
  }
}

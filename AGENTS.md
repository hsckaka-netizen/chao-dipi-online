# 炒地皮在线版项目规则

## 任务启动

1. 先读 `docs/project-memory.md`；它只是文档路由，不包含全部业务规则。
2. 根据其中的映射，只读与当前需求直接相关的模块文档。
3. 先搜索再读取代码，只打开命中的相关片段；不要默认通读大文件、全部文档或旧任务。
4. 仅在判断现状需要时查看少量近期提交，不默认展开完整 Git 历史。

## 修改原则

- 保持需求范围，不做无关重构，保留工作区内不属于当前需求的改动。
- 统计、账号、历史、钻石等旁路能力不得改变或阻塞牌局主流程。
- 浏览器只展示结果；账号权限、牌局规则、奖励和数据库写入必须由服务端校验。
- 密钥只允许存在于服务端环境变量，不能写入前端、日志或提交。
- 修改稳定规则、数据口径、部署方式或已确认交互时，只更新对应模块文档。

## 验收

- 纯文案或不影响布局的微小 UI 改动：语法检查和直接相关测试。
- 布局、交互或响应式改动：除语法检查外，实际检查受影响页面；涉及响应式时至少检查一个桌面尺寸和一个目标移动端尺寸。
- 规则或服务端改动：运行直接相关测试。
- 结算、数据库、实时同步、认证或跨模块改动：运行完整测试。
- 素材或前端资源变化后，更新并检查内容版本。

## Git 与发布

- 需求任务优先使用独立 Worktree；不得直接推送 `main` 或部署。
- 需求完成并准备交接时，形成独立提交，并报告提交号、修改范围、检查结果和风险。
- 每个发布批次使用一个新的发布任务；发布任务按明确提交号整合、复核、推送 `main` 并检查生产环境。
- 只有用户明确要求发布时，才允许推送 `main` 或触发部署。

## 沟通

- 需求明确时直接实现，不重复复述，不输出长计划。
- 过程只保留简短进度；最终只说明结果、检查、提交号和必要风险。
- 发生中断且有未完成状态时，才创建不超过 30 行的 `handoff.md`；任务正常完成后不保留。
